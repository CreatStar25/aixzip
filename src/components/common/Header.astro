---
import { getLangFromUrl, useTranslations } from '../../i18n/utils';
import { navData, ui } from '../../i18n/ui';

const lang = getLangFromUrl(Astro.url);
const menu = navData[lang] || navData['en'];

const languageNames = {
  'en': 'English', 'zh-cn': '简体中文', 'zh-tw': '繁體中文', 'es': 'Español',
  'ar': 'العربية', 'pt': 'Português', 'id': 'Bahasa Indonesia', 'ms': 'Bahasa Melayu',
  'fr': 'Français', 'ru': 'Русский', 'hi': 'हिन्दी', 'ja': '日本語',
  'de': 'Deutsch', 'ko': '한국어', 'tr': 'Türkçe', 'vi': 'Tiếng Việt',
  'th': 'ไทย', 'it': 'Italiano', 'fa': 'فارسی', 'nl': 'Nederlands',
  'pl': 'Polski', 'sv': 'Svenska', 'uk': 'Українська', 'ro': 'Română'
};
const locales = Object.keys(languageNames);

// ✨✨✨ 核心修复：智能路径生成函数 ✨✨✨
const { pathname, search } = Astro.url;

const getLocalizedPath = (targetLang) => {
  // 1. 将路径拆分为数组，过滤空项
  const segments = pathname.split('/').filter(Boolean);
  
  // 2. 如果当前不是英文，且路径第一段是当前语言代码，则移除它
  // 例如：从 /zh-cn/image/tool 变成 [image, tool]
  if (lang !== 'en' && segments[0] === lang) {
    segments.shift();
  }
  
  // 3. 如果目标不是英文，则在头部添加目标语言代码
  // 例如：变成 [es, image, tool]
  if (targetLang !== 'en') {
    segments.unshift(targetLang);
  }
  
  // 4. 重新组合路径，并保留 URL 查询参数 (例如 ?q=pdf)
  return '/' + segments.join('/') + search;
};
---

<header class="bg-white/90 backdrop-blur-md border-b border-slate-100 sticky top-0 z-50 transition-all">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-20 flex items-center justify-between">
    
    <a href={`/${lang === 'en' ? '' : lang}`} class="flex items-center gap-2 group flex-shrink-0">
      <div class="w-10 h-10 bg-blue-600 rounded-xl flex items-center justify-center text-white text-xl font-black shadow-lg shadow-blue-500/30 group-hover:scale-105 transition-transform">
        <i class="fa-solid fa-box-open"></i>
      </div>
      <span class="text-2xl font-black text-slate-800 tracking-tight">
        AI<span class="text-blue-600">x</span>ZIP
      </span>
    </a>

    <div class="hidden md:flex gap-8 h-full items-center">
      {Object.entries(menu).map(([key, category]) => (
        <div class="group relative h-full flex items-center cursor-pointer">
          <span class="text-base md:text-lg font-extrabold text-slate-600 group-hover:text-blue-600 py-6 transition-colors tracking-wide flex items-center gap-1">
            {/* @ts-ignore */}
            {category.title} 
            <i class="fa-solid fa-chevron-down text-[10px] opacity-40 group-hover:opacity-100 transition-opacity mt-1"></i>
          </span>
          <div class="absolute top-full left-1/2 -translate-x-1/2 w-[280px] bg-white shadow-xl rounded-2xl border border-slate-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform translate-y-2 group-hover:translate-y-0 p-3 grid gap-1 z-50">
             {/* @ts-ignore */}
             {category.items.map(item => (
                <a href={item.href} class="block px-4 py-3 rounded-xl hover:bg-slate-50 text-slate-600 hover:text-blue-600 font-bold text-sm transition-colors flex items-center gap-3">
                    <i class="fa-solid fa-caret-right text-slate-300"></i> {item.name}
                </a>
            ))}
          </div>
        </div>
      ))}
    </div>

    <div class="flex items-center">
        <div class="group relative h-full flex items-center z-50">
            <button class="flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-slate-900 px-3 py-2 rounded-lg hover:bg-slate-100 transition-colors">
                <i class="fa-solid fa-globe"></i> 
                <span>{languageNames[lang]}</span>
                <i class="fa-solid fa-chevron-down text-xs"></i>
            </button>

            <div class="absolute top-full right-0 w-[600px] bg-white shadow-2xl rounded-2xl border border-slate-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform translate-y-2 group-hover:translate-y-0 p-6">
                <div class="text-xs font-bold text-slate-400 uppercase mb-4 tracking-wider border-b border-slate-100 pb-2">Select Language</div>
                <div class="grid grid-cols-3 gap-x-4 gap-y-2">
                    {locales.map((l) => (
                        <a 
                            href={getLocalizedPath(l)} 
                            data-lang-link={l}
                            class={`text-sm px-3 py-2 rounded-lg transition-colors flex items-center justify-between ${lang === l ? 'bg-blue-50 text-blue-600 font-bold' : 'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}
                        >
                            {languageNames[l]}
                            {lang === l && <i class="fa-solid fa-check text-xs"></i>}
                        </a>
                    ))}
                </div>
            </div>
        </div>
    </div>
  </nav>
</header>

<script>
  const links = document.querySelectorAll('[data-lang-link]');
  links.forEach(link => {
    link.addEventListener('click', (e) => {
      const lang = link.getAttribute('data-lang-link');
      if (lang) {
        localStorage.setItem('user_lang', lang);
      }
    });
  });
</script>